# Copy this file to .env.local and configure your preferred AI provider(s).

# --- FREE: Groq (Llama 3.3 70B) ---
# Sign up for free at https://groq.com → API Keys → Create Key
# Users can also supply their own Groq key via the Settings modal in the UI.
GROQ_API_KEY=your_groq_api_key_here

# --- PREMIUM: Anthropic (Claude) ---
# Sign up at https://console.anthropic.com → API Keys → Create Key
# Enables prefill and Claude-specific features. When set, takes priority over Groq.
# Users can also supply their own Anthropic key via the Settings modal in the UI.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
